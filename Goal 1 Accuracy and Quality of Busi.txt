return memberList.stream()
                .map(member -> {
                    C c = new C();
                    c.setName(member.getName());
                    c.setAge(member.getAge());
                    return c;
                })
                .collect(Collectors.toList());
    }
